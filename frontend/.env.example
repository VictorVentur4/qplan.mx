# Copia este archivo como frontend/.env
# Debe coincidir EXACTAMENTE con el origen desde el que sirves el frontend,
# y estar incluido en CORS_ORIGINS del .env de la raíz.
REACT_APP_BACKEND_URL=http://localhost:8000
